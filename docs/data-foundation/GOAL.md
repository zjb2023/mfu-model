# 当前执行目标 · 2026-09-21

## 最新阶段状态

2026-09-22副本扩展设计已记录于DP_EDP_REPLICA_SCALE_DESIGN_R1.md：固定PP16/EP8/CP2及每rank工作量，全局batch与Dense-DP/EDP同比扩大40倍，目标每stage640卡、80个EP8组；首版假设PP1～PP15 RS完全隐藏，只计PP0暴露尾段。局部F/B复用，max仅在实际普通/专家同步组就绪汇合处出现；相同模板复制不产生额外max惩罚，波动候选需要独立证据。此为设计，未实现新规模预测。用户要求提交推送当前研究更新，保留未相关的历史失败输出不纳入提交。

2026-09-22 r5跨迭代统计已完成：256/224/32各rank0 iter5..100共20轮；启动均值1206.196/1549.707/117.157ms，首F前小AG均值1073.545/85.616/15.367ms。附排除iter100的19轮敏感性。14个16卡case选iter8..103，其中8211缺98，共279份trace；无forward_step同口径启动标记，启动均值N/A。最终以最后CPU统计AG精确External id关联GPU，10～17个有效样本/case，不能把缺关联最后AG换成前一次。初版startup-cohort-r5及host-supplement的16卡均值被startup-cohort-r5-16-verified替代。最终339份trace聚合在startup-cohort-r5-summary，文档STARTUP_COHORT_R5.md，HTML df-v001/startup-cohort-r5/；339/17覆盖、六个既有边界回归、全部清单哈希和桌面/手机网页检查PASS。16卡精确关联覆盖PARTIAL，未知不记0。这里只是rank0跨iter均值，不是全rank均值，iter60等待占比不自动泛化到20轮。预测未改，未提交推送。后续应区分224进入AG前耗时与256进入后等待，不能仅凭AG均值拟合规模系数。

2026-09-22 r4全rank到达诊断：限定256卡iter60的256份trace（8.894GB，只提取启动AG/前置barrier），输入SHA256、覆盖、消息量及CPU/GPU关联全部PASS。关键晚到组worker34009 rank80～87：前置barrier后约1.15秒才进入AG，自身驻留2.877～8.335ms；rank0驻留1068.895ms。基于同一barrier标准完成语义、只用rank本地时间差，最后参与者至少晚1059.162ms，rank0在所有kernel开始后剩余≤9.733ms。支持到达不齐主导，不等于100%纯等待或残余全是传输。rank83主要停顿在CPU发起AG前的未记录主机区间，CPU发起到GPU开始仅0.135ms。显存report/gmi为具体候选，未证明因果。文档STARTUP_ARRIVAL_R4.md及STARTUP_LATE_HOST_R4.md、结果startup-arrival-r4、HTML df-v001/startup-arrival-r4/已落盘。下一步若继续解释晚到来源，需实际主机调用/子进程时序；不按rank数或带宽硬拟合，不修改正式预测。

2026-09-22启动工作清单r3（当前最新）：完成32/256各iter40/60/80 rank0六样本七段守恒与事件清单；确认5次按层小AllReduce、两次全局barrier、world×24计时AG及24次筛选、两块大梯度缓冲清零。236B与实际32卡日志强匹配MoE辅助统计/计时汇总，不是token路由分发的证据。iter60主机记录稀疏段83.612/88.130ms，清零GPU约4.80/4.85ms；小AG的秒级驻留与CPU读取阻塞重叠，不重复计费。见 `STARTUP_WORK_R3.md` 和 `startup-work-r3-verified`；新增HTML `df-v001/startup-work-r3/`，原总览底部增加入口。正式参数/预测未改。唯一后续：代表性跨PP rank的同一计时AG就绪诊断，严格保留时钟约束；主机残余与实际调用栈仍缺证据。不按总参数量或world统一缩放。

2026-09-22最新进展（以下历史“下一步”不再代表当前优先级）：已按用户纠正核对整个Profiler启动窗口，而非将AllGather等同启动。32/256卡各iter40/60/80共六份rank0 trace边界、分段守恒检查通过；iter60整个启动107.177/1196.823ms，差1089.646ms，其中首F前小全局AG驻留差占时间分段差约97.9%。这是定位证据，不证明纯传输、纯等待或rank数独立因果关系。详见 `PROFILER_STARTUP_R2.md`。

尾段60%带宽简化候选已写入prediction-overview-r2页面底部：296.626＋51.505＝348.131ms，再加独立同步项；相对实测376.997ms的28.867ms差额不直接命名为同步。启动三轮比较同步展示。浏览器无脚本错误，390px无横向溢出，原5行整体诊断/4行专家诊断保留，三个证据链接HTTP200；研究manifest四项及网页manifest六十项哈希通过，服务镜像与仓库HTML一致。正式预测未改，本批未提交推送。

状态：窗口与网页验证PASS，因果建模PARTIAL。唯一下一步：确认首F前小全局AG的实际框架调用身份及各rank就绪关系，区分统计/同步与传输；实际采集代码、调用栈或跨rank时钟不足时保留有效启动参数，不按rank倍数或60%带宽硬推整个启动窗口。目标NIC规格与同步独立成本仍为尾段候选缺口。

尾段peer r2已完成：顶层pg_config确认EDP为全局[0,8]/[1,9]跨worker；AG每组两次独立step_with_ready_grads调用，单tensor全量，不是重复事件或半量bucket。同组DP RS rank0约23.8ms、rank8约129.9ms，不能用驻留反拟合纯带宽。启动小AG四rank均约1.07s，疑似timer统计汇总但调用栈未证明。详见 `TAIL_PEERS_R2.md`。下一步显式普通DP/EDP分支与两轮AG依赖，目标NIC/机内带宽及就绪仍需核验。

启动/尾段下一步已执行：DP RS组8→16、新增EDP2 RS身份及字节量确认；源NIC记录400Gb/s，η60%给出30GB/s。两机8卡分层假设下DP跨机RS/AG约9.932/4.966ms、EDP2约31.457/15.729ms，仅服务候选，非完整尾段。启动段发现96字节/rank全局AllGather在目标驻留1068.895ms，不能按带宽解释。详见 `TAIL_DP_EDP_60_R1.md`。下一步核对目标映射、AG bucket和网卡共享；启动到齐等待独立处理。未改冻结模型/网页，未提交推送。

2026-09-22最新用户优先级覆盖以下历史“下一步”：暂停以预知token路由分布改进专家成本外推，保留F/B误差归因作为已知限制。主线转为PP0首F前启动段及PP0末B后的RS→优化器→AG尾段。通信候选固定利用率η=0.60，即B有效=0.60×独立核验的链路理论带宽；不是“目标带宽为源带宽60%”。核对普通参数DP with CP（源8→目标16）和EDP（源1→目标2）的RS/AG组成员、消息量、机内/跨机路径，补新增专家副本同步；优化器计算、等待、启动软件开销不能按带宽统一缩放。已有rank0尾段源296.626ms、目标376.997ms，差80.372ms；现有缓存缺独立字节量和绝对带宽，不能复用旧带宽比例敏感性冒充新60%模型。唯一下一步：从既有rank0 trace元数据提取DP/EDP RS/AG字节量与组身份，并核验硬件带宽来源，再做独立候选；不改冻结模型、不使用目标总时长反拟合带宽。

2026-09-22中间F专家子项替换已完成：在B两类替换固定后，F仅FC1/激活/FC2替换额外减少272.528ms，1F1B由20.564812秒（+0.8276%）变为20.292283秒（−0.5086%）。正常F无GroupedLinear标记，采用permute→silu→unpermute边界＋GPU关联识别，未替换整个F。证据见 `F_EXPERT_TRANSFER_R1.md`；仍为代表rankoracle诊断，不是新预测精度，未改模型、网页或推送。

2026-09-22按用户新要求细化token/Attention贡献：FC固定替换后剩余426.552ms中，确认token permute/unpermute贡献101.511ms、专家激活/门控156.497ms，合计60.49%；确认Attention非CP＋旋转编码29.952ms（7.02%，非整个Attention）。未确认模块身份的线性65.462ms、拷贝24.566ms继续单列。256组合及旧窗口回归通过，详见 `TOKEN_ATTENTION_R1.md`。不将DeepEP全部打包解包宣称已覆盖；本轮未提交推送。

其余B成本排他性时间归因已完成一轮：9类512组合，Shapley总和1104.672ms；FC838.720ms、其他设备活动300.503ms、重叠75.280ms、组起点45.219ms、Attention独占25.884ms，CP/EP/无设备记录间隔为负向抵消。旧FC直接替换830.793ms回归通过。仍是代表rank外层诊断，详见 `B_REMAINING_R1.md`。下一步优先细化300.503ms“其他活动”的框架父算子身份，不把剩余项统称等待。

已完成中间14stage×4B的FC代表rank成本替换诊断：只换真反向FC减少600.985ms（中间B oracle的54.40%），连同重计算FC减少830.793ms（75.21%）；剩余1F1B误差约2.09%。32组合与基线回归通过，原参数未改。此为有效B加性分项替换后外层DAG调度，不是完整EP8内图替换，结论PARTIAL，详见 `FC_TRANSFER_R1.md`。下一步核查完整EP8成本替换的并发/关键路径敏感性。

按最新要求先对齐PP1、暂缓跨PP拟合：完成iter60 B0源/目标八卡专家形状与代表rank算子/kernel核验。Attention+CP设备并集相近；专家真反向活动差32.757ms，内部未覆盖间隔仅差0.535ms；八卡FC2输入行数之和也有明显差异，原因尚待路由/容量/实际实现核验。PP1 B0–B3误差排名4/2/3/1，并非全部最小。证据与复现见 `PP1_ALIGNMENT_R1.md`；下一步先解释专家工作量差异，再研究跨PP波动。未改冻结模型或推送。

R11后续总览复算已完成：32卡120609节点回归通过，256卡421997节点/112实例重新装配通过；1F1B仍21653.358ms（+6.164659%），ProfilerStep仍22057.161ms（+0.397486%），派生MFU4.582876%。默认参数未校准，不能称精度改善。原总览URL已更新R11入口、复算说明和证据口径，详见`PREDICTION_R11_RECALCULATION.md`。

最新用户要求：重计算不得继续整段保留。已实现R11候选，将32个层/rank重计算包络换成计算、CP2、EP8成本子图；默认B868.833ms与R10一致，136项扰动及网页编辑检查通过。旧R10与正式外推预测保留，不宣称新跨迭代精度。网页`/df-v001/b-recompute-r11/`，详见`B_RECOMPUTE_R11.md`。下一步为源目标专家FC工作量与分项成本核对，再做独立评价；以下尾段及归因计划保留为历史记录和待办。

用户要求暂时闭合F/B误差分析并入Git，已完成冻结提交 `8e55d28`（32to256，本地，未推送）。总览及索引页面176文件、105本地链接已归档并核验；含逐B40/60/80证据。B差异解释大部分iter60高估，物理原因和跨迭代预测仍不宣称闭合。

当前转入优化器尾段：首轮源32/目标256 rank0对照已完成，尾段差80.372ms，其中62.117ms位于末B→最后RS窗口。见 `OPTIMIZER_TAIL_AUDIT_R1.md`。唯一下一步为RS的DP/EDP身份、消息量和就绪/完成核对，不继续拟合F/B整体k。以下为历史计划和进度记录。

## 优化器尾段待办（最新补充，未实施）

- [ ] 区分通信结构变化与带宽变化：32卡普通参数DP with CP=8、EDP=1；256卡普通参数DP with CP=16、EDP=2。新增专家副本同步不能只用降低原DP通信带宽代替。
- [ ] 核对trace中源1个RS、目标2个RS的DP/EDP身份及对应AG、消息量与rank组；段数是已观察事实，具体事件身份待验证。
- [ ] 普通参数与专家参数分别接入通信成本；核实rank到机器映射后，按机内或机内＋跨机混合模式使用不同有效带宽。优化器计算和等待独立保留，按依赖与重叠调度，不直接相加所有通信时长。
- [ ] 对比简单候选与冻结源尾段复用方案，报告能解释的差值及残余；目标实测仅用于评价，不以尾段总差值反拟合带宽。现有带宽敏感性结果不是完整DP/EDP尾段模型，正式预测不变。

详见 `OPTIMIZER_TAIL_AUDIT_R1.md` 的“DP/EDP结构差异待办”。本次仅记录，不启动新实验。

## 目标

定位已冻结32→256细化F/B外推的1.257秒（6.164659%）偏大误差，区分F/B成本迁移与PP成本/依赖连接的影响，形成可追溯的归因图和下一版唯一修改建议。今天不以强行降低误差为验收目标。

用户已明确设定为今天目标。文档状态：推进中；按最新讨论优先评价PP成本复用，暂不执行三组F/B诊断替换。工作区 `/home/zjb/Desktop/worktrees/mfu-16to256`，分支 `32to256`（目录仍保留旧名）。冻结基线提交 `0f14f35cf0cb4586efd5078d5ff3d6d4aaf9a558`。

## 持久目标状态冲突

系统目标仍是“持续优化2111／16卡、iteration53校准预测58”，状态paused；不是本目标，也没有被标记完成。当前目标接口只能创建目标、查询及标记complete/blocked，不能取消或替换未完成目标。今天任务以本文档为执行依据；系统持久目标尚未切换，不声称已激活新系统目标。旧目标与其历史成果保留。

## 事实基线

- 32卡iter70首F→末B区间误差2.796075%。
- 256卡iter60 rank0同口径：预测21653.357840ms、实测20396.013187ms，偏大1257.344653ms；PP0首B结束时已偏大约1027ms。
- 以上含流水线填充/排空，不含RS/OPT/AG，不是完整MFU或全世界逐rankmakespan。
- 9月18日旧r7归因：用目标全部F/B作诊断替换后剩余约−141.509ms，提示不能先认定PP为主因。旧结论不自动当作当前模型的归因结果。

## 检查清单

- [ ] 核验原工作区既有256逐stage缓存、消息/边界和来源哈希，准备只读评价输入清单；不默认旧绝对路径已在新分支落盘。
- [ ] 固定当前图及源参数，复算原预测，确认与封存一致。
- [ ] 在独立诊断评价器比较原预测、仅替换中间F/B、替换全部F/B三组；目标实测仅用于诊断，不能晋升为预测成本。
- [ ] 沿PP链检查首次F/B边界偏差积累，区分块自身时长和块间连接差异；跨机时间不可比时使用rank局部对齐并标明限制。
- [ ] 输出HTML误差归因图、诊断对照表、已解释/未知项及下一版唯一修改建议。
- [ ] 保存代码、输入许可、manifest/哈希、报告、复现命令与验证结果；检查源图和参数未变。

## 验收

1. 冻结原预测可复现，三个诊断条件只改变已声明因素，输出可核验。
2. 明确给出F/B替换对最终误差的影响和剩余差异；剩余差异不能直接命名为纯PP通信误差。
3. 图能说明约1.03秒偏差沿哪段累积；无法确定的部分标明证据缺口，不强行分摊。
4. 明确下一版应优先核对F/B迁移还是PP建模；本日不要求新增拟合或提高预测分数。

## 边界

- 不修改冻结预测、不使用256数据回填参数；诊断替换不是独立预测或盲测精度。
- token驱动FC1/FC2拟合保持暂缓；不扩大kernel分类、不加入更新尾段。
- 14个16卡case及224卡PP校准只作为后续选项，不在归因前全面铺开。
- 原数据和旧成果只读；新输出独立目录，不覆盖。未经另行要求不提交、推送、合并或启动新agent。

## 进展、阻塞与唯一下一步

已完成：PP成本复用抽样评价r1，见 `PP_TRANSFER_EVALUATION_R1.md`。10个rank原始trace及目标GPU结束缓存核验通过；复现32卡PP冻结参数，目标3条链路的独立收发有效窗口F/B中位数4.933/5.198 ms，高于源2.762/2.725 ms。相同80 MiB payload；跨worker路径代理，非纯网络成本证明。模型图及源参数未改。三组F/B替换及整体误差归因仍未执行，不标记完成。
已知阻塞：系统目标切换接口受限；不阻止按本文档推进研究。精确物理路由、跨机时钟及全rank并发未核验。
后续进展：用户明确要求简单PP规模经验系数，并考虑14个16卡case。已建立单参数独立候选，PP16倍率1.84582，整体窗口增加83.604 ms，误差6.16466%→6.57456%；这是目标数据校准诊断，未晋升或修改冻结预测。已完成14case输入盘点，8个有PP、6个不适用；16卡系数尚未拟合。详见 `PP_SCALE_CANDIDATE_R1.md`。
唯一下一步：先核对2211/2411同80MiB消息的路径与GPU就绪，建立PP2→4的配置族经验倍率，再用iter58检查稳定性；不把CPU调用时长直接当传输成本。

2026-09-21最新用户优先级：先整合预测任务全景，暂缓内部拟合。已发布 `prediction-overview-r1`，整合原F/B交互图、16-stage DAG、PP成本、启动与更新尾段；数据哈希及浏览器检查PASS，整轮独立预测仍N/A，1F1B误差6.164659%。详见 `PREDICTION_OVERVIEW_R1.md`。本次资料整合已交付；后续等待用户审阅，再讨论PP1/PP2基准和1F1B拟合，不自动启动前述14case校准。

随后用户明确要求用32卡启动和尾段预测整轮MFU：已完成源成本倍率1整轮候选，页面 `prediction-overview-r2`。ProfilerStep22.057161秒（+0.397486%）；继承源日志时钟残余后TrainingStep24.512649秒（+5.151249%），MFU4.123799%对同口径4.336226%。1F1B仍+6.164659%；整轮低误差有启动低估抵消，不能算内部拟合完成。状态PARTIAL（有效FLOPs/峰值继承、尾段规模变化未物理建模），详见 `FULL_STEP_32TO256_R1.md`。不自动晋升或覆盖冻结主模型。

最新执行结果：统一ProfilerStep为主指标后，按用户要求完成当前细化图1F1B高估归因。中间B贡献1104.672ms，中间F241.769ms，首尾52.412ms，残余−141.509ms；只替换中间B的目标实测成本，诊断误差降至+0.748542%，不得宣称新的预测精度。16组消融、缓存/边界检查和网页诊断面板已落盘，见 `ONE_F_ONE_B_OVERESTIMATE_R1.md`。主预测及k不改。唯一下一步是源PP1/PP2 B成本与B内分项差异核对，而非直接拟合整体系数。
