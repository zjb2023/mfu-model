# PP1先对齐，再研究跨PP波动

## 范围与复现

仅比较iter60、PP1、B0。算子细项取32卡rank8与256卡rank16；专家输入额外核对源rank8–15、目标rank16–23八卡。四个单元按B执行顺序编号，未通过权重ID证明全局层身份。未读取目标第二EP8组，未修改模型成本。

```bash
cd /home/zjb/Desktop/worktrees/mfu-16to256
python -B workflow/data_foundation/compare_pp1_alignment_r1.py --out results/data-foundation/pp1-alignment-r1-reproduction
```

输出目录必须不存在。本次只读16个rank trace。结果 `results/data-foundation/pp1-alignment-r1/`；manifest记录输入路径、SHA256、代码与输出哈希。详细算子记录含CPU/设备事件索引、输入形状、完整kernel名称、计数和时间。

## PP1是否最接近

以源B成本868.833359 ms比较目标14个中间stage，PP1的B0–B3绝对误差由小到大排名分别为4、2、3、1。PP1属于较接近的stage，但不是所有B都最小，不能假设跨PP单调变快。

PP1 B0实测816.151993 ms，源组成本高52.681365 ms。源rank8局部包络866.173446 ms，另有组起点偏移2.659913 ms；组成本与单rank包络不能混同。

## 算子层比较

每类四层调用关联设备活动并集，单位ms。Attention包含CP，不是纯计算；专家线性不包含全部MoE工作。

| 分项 | 源32卡 | 目标256卡 | 源减目标 |
|---|---:|---:|---:|
| 重计算Attention＋CP |186.509|186.702|-0.194|
| 真反向Attention＋CP |284.900|282.275|2.626|
| 重计算专家FC1 |13.900|5.376|8.524|
| 重计算专家FC2 |7.353|2.898|4.456|
| 真反向专家FC1 |33.368|12.351|21.018|
| 真反向专家FC2 |18.748|7.009|11.739|

专家真反向FC1＋FC2活动差32.757071 ms；这两个算子包络内部未覆盖间隔差仅0.534677 ms（源0.791276，目标0.256600）。此子项差异主要位于GEMM活动，而不是内部等待。重计算专家线性活动另差12.979913 ms。

这些不是1F1B关键路径贡献，不能直接相加除以52.681 ms或1.257秒来宣称解释比例。

## Kernel证据

- FC1真反向关联kernel数148→48；FC2也148→48。重计算两个FC分别74→24。
- 同名 `musa_asm_bf16bf16ssssgemm_tn_tce_512_256x256B128_epilogue_mldz_persis_stage2`：FC1中74次/19.833 ms→24次/7.067 ms；FC2中74次/10.694 ms→24次/3.833 ms。这是kernel时长累计，不是B墙钟。
- 真反向Attention CP SendRecv均20次，累计173.915→172.041 ms；flash-attention backward均4次，累计87.234→85.380 ms。
- Attention内 `CopyLastContiguousKernel` 均20次，累计18.535→36.394 ms，目标反而更长。存在重叠，Attention设备并集未同比增长，不能将kernel累计时长直接相加。

## 八卡专家输入

同一执行单元八卡FC2反向输入第一维之和；第二维均5120。这不是独立样本token数，尚未证明等于未经复制/丢弃的路由总量。

| B内执行单元 | 32卡八卡行数之和 | 256卡八卡行数之和 |
|---|---:|---:|
| 0 |257746|121858|
| 1 |276958|137872|
| 2 |276544|132947|
| 3 |262897|149900|

差异不只发生在单rank，但成因未闭合。此前rank0日志显示两边EP8、CP2、ETP1、160专家、top-k6、MBS2、seq8192、专家中间维1536相同，仍不足以证明路由、非空专家数和实际实现一致。不得直接归因于EDP2、CP消息256/384档位或计算效率提高。

## 等待口径与结论

真反向EP Combine关联设备包络内部未覆盖时长：源14.423 ms、目标25.434 ms，目标反而多11.011 ms。这不是纯等待证明，可能有其他设备活动或不可见传输。CPU调用时长和少量关联kernel并集都不能代替完整网络服务成本。

PASS：限定样本的形状、算子、kernel关联与哈希核验。PARTIAL：原因归属、全MoE排他性墙钟分解及跨迭代稳定性尚未完成。

下一步核查dispatch输出、每专家token计数/容量处理及实际GroupedLinear实现，解释PP1专家工作量与GEMM次数差异，再研究跨PP波动。旧6.164659%预测与中间B oracle结论不变。未提交、推送或修改冻结成本。
