# R11重新装配与总览更新

使用R11重计算＋真反向B子图重新展开源32卡与目标256卡流水线；F、源首尾成本、PP成本及MFU口径保持不变。不是只更换页面标签。

## 复算结果

| 指标 | 预测 | 目标实测/同口径推导 | 相对误差 |
|---|---:|---:|---:|
| 256卡1F1B | 21653.357840ms | 20396.013187ms | +6.164659% |
| ProfilerStep | 22057.160633ms | 21969.833588ms | +0.397486% |
| ProfilerStep派生MFU | 4.582876% | 4.601093% | −0.395912% |

与R10默认1F1B相差约−8.7e−11ms，仅浮点误差。R11拆解了成本接口，没有校准默认成本；没有精度改善结论。整轮低误差仍有启动低估抵消1F1B高估。

## 实际执行的检查

- 32卡PP4/m8展开120609节点、32个中间F/B实例，窗口11917.227759ms，与原32回归一致。
- 256卡PP16/m4展开421997节点、112个中间F/B实例。所有外层节点与收缩DAG完成时间匹配，最大差约2.3e−10ms。
- 所有F/B包装成本归零，避免重复计费。
- s1:B0重计算FC1增加100ms，在该完整流水线中被余量吸收，整段变化0；增加1000ms后整段增加313.375ms，证明新成本路径已接入外层调度。初次测试误将+100ms必然改变整段作为断言，已修正为非负检查及更大的传播压力测试后重跑通过。
- 先封存新预测，再读取旧目标缓存评价；本轮不重扫trace，不重新训练，不用目标成本调参。目标iter60此前已曝光，不是盲测。
- 新页面桌面/手机无溢出、无JS异常，B链接/iframe指向R11，B证据索引保留原历史诊断口径。

## 归档与复现

结果：`results/data-foundation/prediction-r11-recompute-r1-verified/`。前次失败目录不代表完成结果。

展开图在内存中实际调度；落盘采用因子化形式：F/B模板的绝对路径与SHA、构图代码、外层图、112个绑定、参数来源和封存结果。避免重复保存数十万份相同内层节点，仍可由记录的compose函数完整复现。

```bash
python -B workflow/data_foundation/recompute_prediction_r11.py --out /tmp/prediction-r11-repeat
```

输出目录须不存在。网页更新脚本`publish_prediction_r11_refresh.py`为一次性发布，旧index/manifest/report已备份，不重复覆盖。

网页保持原入口：<http://192.168.8.16:43312/df-v001/prediction-overview-r2/#b-evidence>。

状态仍为PARTIAL条件外推：只代表EP8 cohort、假设EDP2另一个cohort等价；首尾及PP成本未重新建模；MFU继承历史有效FLOPs与峰值口径。未提交、推送。
