# 32卡 F 内拆出 CP：最简示意 r1

2026-09-18；工作区 `/home/zjb/Desktop/worktrees/mfu-w37-v610`。

## 结论

外层F/B与PP依赖保持，F节点的原整体成本由子图替换，不能额外叠加通信成本。第一步独立CP，EP仍保留在显式标注的融合剩余块M中。

源32 iter60 PP1 F0，rank8～15：CP组为[8,9]、[10,11]、[12,13]、[14,15]；EP组覆盖8～15。每rank观测4个重复单元，每单元CP输入序列为384、384、256、256 MiB（BF16，每rank输入张量，不是线上字节量）。按CP调用次序定义单元，不冒充已确认的框架层编号。

示意每单元：C0→CP0→C1→CP1→C2→CP2→C3→CP3→M。

- C为每rank本地工作，不宣称全部是纯计算，GPU就绪边界待验证。
- 每个CP是两rank共享节点，不添加全8rank屏障；拟采用配对就绪max近似，真实异步GPU依赖尚未确认。
- M含剩余本地处理及EP dispatch/专家/combine，跨8rank关系尚未展开，不能假设它是独立纯计算。
- 候选CP共享成本只有两档，但同尺寸不同调用是否可共享仍需验证。cost均留空，不填写实测包络为纯service。
- 等待由依赖产生，不能同时添加到CP成本、计算成本和额外等待参数。
- 已观测CP与本地设备工作重叠；必要旁路要由真实依赖证明，不在本图中猜测。当前图是结构提案，不是已闭合可预测模型。

## 交付

网页：http://192.168.8.16:43312/df-v001/pp32-cp-demo-r1/

默认14个节点，切换4对CP rank和4个重复单元，点击或键盘激活节点查看参数说明。按lark-apps图表规范沿用本地SVG/页面风格；不发布云端，不提交Git。

脚本：`workflow/data_foundation/publish_pp32_cp_demo.py`。

模板：`workflow/data_foundation/ui_pp32_cp_demo.html`。

产物：`results/data-foundation/2111-blocks-ui-r1/df-v001/pp32-cp-demo-r1/`，包含HTML、contract.json、validation.json、manifest.json（源及输出SHA256）。发布目录已存在时拒绝覆盖。

机读提案完整覆盖224节点、280边，其中64个共享CP节点；这是重复展开数量，不是独立自由参数数量。CPU顺序近似与未闭合EP边明确标注，不能据其无环就宣称GPU依赖正确。

## 验证

CP调用规模与r8源事实一致、无环、每CP两前驱检查PASS。浏览器HTTP200；16种选择均正常、默认14节点、节点点击显示对应384 MiB说明；无页面异常，390px无页面横向溢出；完整截图已查看。预测回放未执行、成本未校准，研究状态PARTIAL。

本轮未扫描原始trace、未修改r6/r7/r8或旧v610参数。

下一步：一对rank的一个重复单元，确认计算产物就绪、CP完成及消费者依赖，保留实测重叠；再测试拆分回放与原F窗口的一致性。
