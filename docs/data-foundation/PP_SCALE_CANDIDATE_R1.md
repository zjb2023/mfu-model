# 简单PP规模经验系数 · r1

2026-09-21，按用户要求增加独立候选；未修改冻结模型、未提交或推送。

公式：`t(方向,P)=t32(方向) × [1+α×(P/4−1)]`。

仅一个新参数，F/B共用α。以32/256的F/B有效耗时中位数做等权最小二乘，α=0.281940143，PP16时倍率1.845820429。不是对整体迭代误差反向拟合。两点经验式只适用于当前80MiB、PP4至16范围的候选，不说明PP数量是因果，也不代表其他拓扑通用规律。

候选前向PP成本5.097951 ms，反向5.030566 ms。用冻结F/B模板的精确标量收缩进行调度（基线窗口与冻结结果差小于1e−6 ms），256卡首F→末B从21653.357840 ms增加至21736.962028 ms，增加83.604187 ms；对20396.013187 ms实测的误差由+6.164659%变为+6.574564%。这是使用目标数据的校准诊断，不是独立预测精度。候选保留、不晋升；F/B图和原参数哈希未变。

权威结果：`results/data-foundation/pp-scale-candidate-r1-verified/report.json`，同目录包含候选参数和哈希清单。早期`pp-scale-candidate-r1`保留为初跑记录，其代码此后补充了依赖清单；以verified目录为准。

复现（输出目录必须不存在，不读取原始trace）：

```bash
cd /home/zjb/Desktop/worktrees/mfu-16to256
python -B workflow/data_foundation/fit_pp_scale_r1.py --out /tmp/pp-scale-r1-repeat
python -B workflow/data_foundation/inventory_pp14_r1.py --out /tmp/pp14-inventory-repeat
```

## 14个16卡case的扩展证据

已盘点既有CPU调度缓存，共1408条条件配对，覆盖迭代53/58。8个case有PP，6个PP1不适用。

| 消息量 | PP2 | PP4 | 用途 |
|---|---|---|---|
| 80 MiB | 2211、2212 | 2411、2412 | 首选2211→2411，同组2212→2412复核 |
| 40 MiB | 4211、4212 | 4411 | 4211→4411另组检查 |
| 20 MiB | 8211 | 无 | 暂不能提供同消息量PP2→4倍率 |

盘点结果及输入SHA在`results/data-foundation/pp14-scale-inventory-r1/`。目前只是匹配CPU收发，并非经过GPU就绪校正的传输完成证据，不能直接拟合成上面的PP有效成本。尚未产生16卡经验系数。

下一步：先核对2211/2411的路径、时钟和GPU就绪，采用与32/256一致的有效窗口口径；用iter53估计经验倍率、iter58评价稳定性（已有历史暴露，不称盲测）。PP变化同时伴随DP/层数/争用变化，所得系数只称配置族经验值；不把差异全部归因PP数量，也不混合40/80MiB拟合一个比例。
