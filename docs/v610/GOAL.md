# mfu-model v6.10 接管与迁移

日期：2026-09-09。状态：完成（冻结接入、复现、验收和本地提交）；研究算法与参数冻结，不调参。

## 目标

从 W37 `b8330a0`（清单审计 `146cc97`）接入 v6.10，保留全部有效参数、binding 校准/消融代码、历史报告与图册，在 mfu-model 独立分支复现并本地提交。不推送、不合并、不修改 W37 冻结材料。

本环境仍有误投给 mfu 窗口的 W37 准备目标，状态 paused、归 fab。原生目标接口没有切换/取消操作；保留该目标，不伪报完成。本任务按交接授权使用本文件持续记录。

## 工作区与检查清单

- [x] 接管并完整读取交接、融合计划、参数覆盖和版本说明。
- [x] 原 main `e71ccb0`、两个未跟踪结果目录和 224 分支工作区保留。
- [x] 创建 `/home/zjb/Desktop/worktrees/mfu-w37-v610`，分支 `feat/w37-v610`。
- [x] 审阅 224 分支 `132d657` 的 FLOPs/外推模块：依赖校准总量与旧时序模板；本版保留冻结 FLOPs 常量，暂不引入其策略重分配算法或整仓差异。
- [x] 复验 371 文件清单；外部包共 379 文件（另补 8 个历史网页），逻辑路径、真实路径、SHA、角色和 426 个代码/配置/精简证据快照已登记。
- [x] 独立 Python 环境和只读隔离入口，v610 model/evaluate/render PASS。
- [x] binding 拟合/四组消融 PASS；26 张表解压后逐字节一致，覆盖全部节点成本、时间、绑定、图锁及阶段账本。
- [x] 显式输入 Snakemake、profiler 派生主线及可选通信元数据清点；原分析 workflow 保留。
- [x] 32 项测试、84 项当前页面检查 / 17 链接及 23 项历史页检查通过。
- [x] 精确范围本地提交 `ba7c62d`、`3610ac1`、`9fe55b0`；验收说明见 `ACCEPTANCE.md`，最终证据随本文件归档。

## 数据与科学边界

原始采集数据不复制、不入 Git；本轮先搬入 SHA 固定的派生与报告副本。没有指定新的 raw 存储位置，不移动原始数据；以后搬迁必须保留旧位置软链接并复验内容 SHA。旧 seal 原文保留，新位置写独立映射与运行 seal。

当前版 256→224 是已暴露开发评估。源侧兼容细粒度完整回放、旧时长 floor 的独立物理归属、从 raw 重建全部历史成本仍有缺口。本次复现不消除这些限制。16→256 禁止继承本版 256 时序参数。

## 进展、阻塞与唯一下一步

接入无阻塞。最终结果位于本 worktree `results/v610/release-final` 与 `binding-final`，复现方法见 `README.md`，机读交接见 `run_manifest.json`。外部制品在 `/home/zjb/Desktop/mfu-model-artifacts/w37-v610-b8330a0`。

本次任务结束，无剩余接入动作；不自行继续模型优化或 raw 迁移。后续研究必须另行授权，建议优先源完整兼容图验证。旧 paused 原生 Goal 未变更。
