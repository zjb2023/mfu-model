const {chromium}=require('playwright'),assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path');
(async()=>{const out=process.argv[2];fs.mkdirSync(out,{recursive:false});const b=await chromium.launch({executablePath:'/home/zjb/.cache/ms-playwright/chromium_headless_shell-1228/chrome-headless-shell-linux64/chrome-headless-shell',args:['--no-sandbox']});try{const p=await b.newPage({viewport:{width:1800,height:1200}}),errors=[];p.on('pageerror',e=>errors.push(String(e)));await p.goto('http://127.0.0.1:43312/df-v001/b-four-layers-r10/#b-dag');await p.waitForSelector('#b-moe-dag .dag-node');const audit=[];
for(let u=0;u<4;u++){await p.locator('#layers button').nth(u).click();const result=await p.evaluate(()=>{const counts={};for(const [name,v] of Object.entries(window.bDagViews)){for(const e of v.edges){if(e.path[0]!==e.from||e.path.at(-1)!==e.to)throw Error('endpoints');for(let i=1;i<e.path.length;i++)if(!G[e.path[i]].deps.includes(e.path[i-1]))throw Error('invented edge')}counts[name]={nodes:v.nodes.length,edges:v.edges.length};}const m=window.bDagViews['b-moe-dag'];counts.ep8Incoming=m.edges.filter(e=>e.to.includes('EP8:')).length;return counts});assert.equal(result['b-overview'].nodes,8);assert.equal(result['b-moe-dag'].nodes,49);assert.equal(result['b-attention-dag'].nodes,52);assert.equal(result.ep8Incoming,8);audit.push(result)}
await p.locator('#layers button').nth(0).click();const expert=p.locator('#b-moe-dag [data-node="U0:moe:r11:s0:1"]');await expert.click();assert(!(await p.locator('#dag-cost').isDisabled()));const cost=Number(await p.locator('#dag-cost').inputValue());await p.locator('#dag-cost').fill(String(cost+50));await p.locator('#dag-apply').click();assert(await p.evaluate(()=>current.END.end_ms>base.END.end_ms));await p.locator('#reset').click();assert.match(await p.locator('#total').innerText(),/868\.833/);
await p.locator('#b-moe-dag [data-node="U0:moe:r8:s5:0"]').click();assert(await p.locator('#dag-apply').isDisabled());await p.locator('#b-moe-dag path[data-from]').first().click({force:true});assert.match(await p.locator('#dag-title').innerText(),/连接依据/);
for(const name of ['b-overview','b-moe-dag','b-attention-dag'])await p.locator('#'+name).screenshot({path:path.join(out,name+'.png')});await p.setViewportSize({width:390,height:844});assert(await p.evaluate(()=>document.documentElement.scrollWidth<=innerWidth));assert.deepEqual(errors,[]);fs.writeFileSync(path.join(out,'checks.json'),JSON.stringify({status:'PASS',audit,checks:['all arrow witness paths validated against DAG','4 layers','8 EP8 inputs','20 CP2 shared nodes','graph click cost +50 and reset','locked node','mobile no page overflow','no JS errors']},null,2));console.log('PASS diagram UI');}finally{await b.close()}})().catch(e=>{console.error(e);process.exit(1)});
